---
import "@/styles/global.css";
import { urlDomain } from "@/utils/const";

interface Props {
  title?: string;
}

const { title = "● Sebastian Cortes ●" } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta
      name="description"
      content="Este es mi portafolio donde conoceras un poco más de mí"
    />
    <meta
      name="keywords"
      content="loannesdev, loannes.dev, loannes, loannesdev.netlify.app"
    />
    <meta name="author" content="loannesdev" />
    <meta name="robots" content="index, nofollow" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="preload"
      href="/fonts/SpaceGrotesk.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href="/fonts/NunitoSans.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link rel="canonical" href=`${urlDomain}` />
    <link rel="icon" type="image/svg+xml" href="/man-technologist.svg" />
    <meta property="og:url" content=`${urlDomain}` />
    <meta property="og:type" content="website" />
    <meta
      property="og:title"
      content="Sebastian Cortes ● Desarrollador web front-end"
    />
    <meta property="og:description" content="Portafolio" />
    <meta property="og:image" content="/og-image.webp" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content=`${urlDomain}` />
    <meta
      name="twitter:title"
      content="Sebastian Cortes ● Desarrollador web front-end"
    />
    <meta name="twitter:description" content="Portafolio" />
    <meta name="twitter:image" content="/og-image.webp" />
    <meta http-equiv="Content-Encoding" content="gzip" />
    <meta name="color-scheme" content="light dark" />
  </head>

  <body>
    <slot />
  </body>
</html>

<style>
  html {
    scroll-behavior: smooth;

    /* Dark mode management */
    &:is([theme]) {
      &:is(html[theme="light"]) {
        --display-icon-dark: block;
        --display-icon-light: none;

        color-scheme: light;
      }

      &:is([theme="dark"]) {
        --display-icon-dark: none;
        --display-icon-light: block;

        color-scheme: dark;
      }
    }

    &:is(:not([theme])) {
      @media (prefers-color-scheme: light) {
        &:has(#dark-mode-button:checked) {
          --display-icon-dark: none;
          --display-icon-light: block;

          color-scheme: dark;
        }
      }

      @media (prefers-color-scheme: dark) {
        &:has(:not(#dark-mode-button:checked)) {
          --display-icon-dark: none;
          --display-icon-light: block;
        }

        &:has(#dark-mode-button:checked) {
          --display-icon-dark: block;
          --display-icon-light: none;

          color-scheme: light;
        }
      }
    }

    /* --- */
  }

  body {
    font-family:
      "Space Grotesk",
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
    transition: 0.2s ease;
    transition-property: color, background-color;
    background-color: var(--current-background-theme);
    color: var(--current-text-theme);
  }
</style>
